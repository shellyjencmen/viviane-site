# מסמך מסירה – אתר ויויאן אלבוים

## חשוב: מודל המסירה
הקוד כאן הוא **נקודת פתיחה**, לא פרויקט להמשך עבודה בתוך המאגר הזה.
המפתחת המקבלת אמורה **לשכפל את הקוד ולנתק אותו** מהמאגר המקורי, ולהמשיך
משם בפרויקט עצמאי משלה, בחשבון שלה. ראו הוראות מדויקות בסעיף "איך מתחילים
פרויקט עצמאי" למטה.

## קישורים (מקור, לצורך העתקה בלבד)
- מאגר קוד מקורי (GitHub): https://github.com/shellyjencmen/viviane-site
- אתר חי (גרסת אישור, לא לאינדוקס): https://viviane-site-dx8l.vercel.app

## מה זה
אתר תדמית ולידים בעברית, RTL מלא, שנבנה עבור ויויאן אלבוים – יועצת ארגונית,
עסקית ופיננסית ומאמנת אישית. האתר נבנה לפי מסמך פרומט מפורט (מצורף: `פרומט claude.pdf`),
שכולל את מטרות האתר, קהלי היעד, מפת האתר, שפה עיצובית (לוגו, פלטת צבעים, פונט) וטון כתיבה.
מומלץ לקרוא אותו לפני שמבצעים שינויים, כדי לשמר את השפה העיצובית והמסר.

## מבנה הפרויקט
```
site/
├── index.html          עמוד הבית
├── about.html           אודות
├── services.html        תחומי פעילות (ייעוץ ארגוני, פיתוח עסקי, ייעוץ פיננסי, אימון אישי)
├── products.html        מוצרים ושירותים
├── contact.html         צור קשר
├── accessibility.html   הצהרת נגישות (טיוטה – ראו הערה למטה)
├── style.css            כל העיצוב (משתני צבע בראש הקובץ תחת :root)
└── img/                 תמונות ולוגו
```
אתר סטטי לחלוטין: אין build step, אין תלויות, אין צורך ב-npm install.
כל קובץ HTML עצמאי ומקשר ל-`style.css` ולתיקיית `img`.

## איך מתחילים פרויקט עצמאי (השלב החשוב ביותר)

**1. קבלת גישת קריאה חד-פעמית**
תוזמני כ-Collaborator עם הרשאת קריאה למאגר `shellyjencmen/viviane-site`.
זה מספיק כדי לשכפל את הקוד; לא תצטרכי גישה מתמשכת אליו.

**2. שכפול המאגר למחשב שלך**
```bash
git clone https://github.com/shellyjencmen/viviane-site.git
cd viviane-site
```

**3. יצירת מאגר חדש וריק בחשבון שלך ב-GitHub**
בלי README, בלי .gitignore — ריק לגמרי.

**4. ניתוק מהמאגר המקורי וחיבור למאגר החדש שלך**
```bash
git remote remove origin
git remote add origin https://github.com/<שם-המשתמש-שלך>/<שם-המאגר-החדש>.git
git push -u origin main
```
מרגע זה הקוד חי במאגר שלך, ואין יותר שום קשר טכני למאגר המקורי.

**5. פריסה עצמאית**
מייבאים את **המאגר החדש שלך** (לא את המקורי) ל-Vercel, מחשבון Vercel שלך.
כל `git push` לענף `main` במאגר החדש יפעיל דיפלוי אוטומטי משלך.

## איך מריצים מקומית (לפני השלב הזה, לבדיקה)
```bash
cd site
python -m http.server 9000
```
ואז פותחים בדפדפן http://localhost:9000

## מה עדיין חסר (מסומן בקוד בתווית צהובה `<span class="missing">`)
- שלוש המלצות לקוחות (עמוד הבית)
- משך ומחיר לכל כרטיס במוצרים ושירותים (`products.html`)
- קישורי פייסבוק ואינסטגרם (פוטר, `contact.html`)
- פסקת "סיפור אישי" בעמוד אודות (`about.html`)
- שם רכז/ת נגישות ותאריך עדכון (`accessibility.html`)
- מזהה Google Analytics (הערה בראש כל קובץ HTML, יש להוסיף ולחבר בפועל)

## הערות חשובות לפני השקה
1. **הצהרת הנגישות היא טיוטה** שכתבתי לפי תבנית כללית. יש לאשר אותה מול איש/ת מקצוע
   לנגישות לפני עליה לאוויר, ולהשלים את הפרטים החסרים.
2. **תגית noindex**: כל קובצי ה-HTML כוללים כרגע
   `<meta name="robots" content="noindex, nofollow">` כדי שהגרסה הזמנית לא תאונדקס
   בגוגל. יש להסיר אותה (מכל הקבצים) לפני ההשקה הסופית.
3. **טופס לידים**: מקושר כרגע לטופס Google הקיים
   (https://forms.gle/wUYJXhuP6Tpg8TDf6). בפרומט המקורי צוין שזה שלב ראשון בלבד.
4. **נגישות (WCAG 2.0 AA)**: נבנה לפי הדרישה, כולל ניגודיות צבעים, alt לתמונות,
   ניווט מקלדת ו-skip link. מומלץ לוודא עם בדיקת נגישות מלאה.
5. **תוכן שלי (ה-AI) ולא של ויויאן**: הניסוחים תחת "למי זה מתאים" ו"מה מקבלים"
   בעמוד `services.html`, ותיאורי הכרטיסים ב-`products.html`, נכתבו על ידי על בסיס
   הפרומט ולא אושרו מילה במילה מול ויויאן. כדאי לעבור עליהם איתה.

## פרטי קשר
- ויויאן אלבוים (תוכן, אישורים): viviane.elboim@gmail.com, 050-6519764
- מקור התמונות והלוגו: מצורפים בתיקיית `img/`, נחתכו מתוך קובץ הפלאייר "אימון אישי"
  ומתמונת פרופיל שסופקו מראש.
